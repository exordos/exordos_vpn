name: build

on:
  push:
  pull_request:

jobs:
  Build:
    runs-on: self-hosted
    strategy:
      fail-fast: true
    steps:
      - uses: actions/checkout@v3
      - name: Build Genesis Core
        run: |
          set -eux

          exordos build $(pwd)

          # Temporary upload step is done in the build script
          VERSION="$(exordos get-version .)"
          ELEMENT_PATH="/var/lib/repository/exordos_vpn"

          rm -fr "${ELEMENT_PATH}/${VERSION}"
          mkdir -p "${ELEMENT_PATH}/${VERSION}"
          mv output/exordos-vpn.qcow2 "${ELEMENT_PATH}/${VERSION}/"
          cd ${ELEMENT_PATH}/${VERSION}/ && \
            sha256sum exordos-vpn.qcow2 >> ${ELEMENT_PATH}/${VERSION}/SHA256SUMS
